# server/Dockerfile only COPYs the prebuilt :server:installDist output, so ignore
# everything and un-ignore exactly that tree — the deploy uploads ~the dist, not the repo.
*
!server/build/install/server
